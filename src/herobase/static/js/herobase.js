$(document).ready(function() {
    $(document).on('click', '.box .collapsible', function(e) {
        var headline = $(this);
        var box = headline.closest('.box');
        var content = box.find()
    })
});